# 多角色容量扩容诊断报表系统

一个为不同角色提供容量扩容决策支持的综合性报表系统。

> 🎉 **首次使用**: 查看 [欢迎指南](./WELCOME.md) 5分钟快速上手
> 
> 📚 **文档导航**: 查看 [文档索引](./DOCS_INDEX.md) 了解所有可用文档和脚本
> 
> ⚡ **快速开始**: 阅读 [快速入门指南](./QUICKSTART.md) 一分钟启动系统

## 系统概述

本系统为4个不同角色提供专门的容量扩容诊断报表：

### 🎯 核心问题
**哪些资源需要扩容，什么时候必须扩，优先级是什么**

### 📊 角色视角

#### 1️⃣ CTO视角
- **关注点**: 技术上哪些组件需要扩容，怎么扩？
- **内容**: 组件使用率、预测饱和时间、扩容方案、工作量评估
- **特色**: 卡片式展示，按优先级排序，技术细节丰富

#### 2️⃣ CEO视角  
- **关注点**: 这会不会影响业务，值不值得扩容，什么时候必须扩？
- **内容**: 业务影响评估、风险等级、决策建议、截止时间
- **特色**: 风险等级徽章，业务影响分析，决策导向

#### 3️⃣ 运维总监视角
- **关注点**: 全局容量规划，什么时候扩容，怎么安排？
- **内容**: 全局排序表、扩容时间表、资源需求统计
- **特色**: 表格形式，按周计划，资源汇总

#### 4️⃣ 运维工程师视角
- **关注点**: 具体怎么扩容，我需要做什么，需要多少时间？
- **内容**: 执行步骤清单、验证方法、风险提示、工作量
- **特色**: 展开式卡片，详细执行步骤，风险提示

## 🏗️ 技术架构

### 后端 (Node.js + Express)
- **API端点**:
  - `GET /api/capacity/cto-dashboard` - CTO视角数据
  - `GET /api/capacity/ceo-dashboard` - CEO视角数据  
  - `GET /api/capacity/ops-director-dashboard` - 运维总监数据
  - `GET /api/capacity/ops-engineer-dashboard` - 运维工程师数据
  - `GET /api/capacity/component/:id` - 组件详情

### 前端 (React + TypeScript)
- **组件架构**:
  - `CTODashboard` - CTO视角报表
  - `CEODashboard` - CEO视角报表
  - `OpsDirectorDashboard` - 运维总监报表
  - `OpsEngineerDashboard` - 运维工程师报表
  - `PriorityBadge` - 优先级标签
  - `CapacityIndicator` - 容量指示器

### 数据模型
基于 `metrics_daily` 表的核心指标：
- `current_usage_p95`: 当前P95使用率
- `current_usage_p99`: 当前P99使用率
- `is_saturated`: 是否已饱和(>80%)
- `saturation_severity`: 饱和程度(normal/warning/critical)
- `days_to_saturation`: 预计多少天达到饱和
- `predicted_saturation_date`: 预计饱和日期

## 🚀 快速开始

### 环境要求
- Node.js 16+
- npm 或 yarn

### 方式一：使用自动化脚本（推荐）

```bash
# 1. 运行安装脚本（首次使用）
./setup.sh

# 2. 启动开发服务器
./start.sh
```

### 方式二：手动安装

```bash
# 1. 安装后端依赖
npm install

# 2. 安装前端依赖
cd client
npm install
cd ..

# 3. 创建环境配置文件
cp .env.example .env
cp client/.env.example client/.env

# 4. 启动开发服务器
npm run dev

# 或者分别启动
npm run server  # 后端 (端口 3001)
npm run client  # 前端 (端口 3000)
```

### 访问应用
- 前端应用: http://localhost:3000
- 后端API: http://localhost:3001

### 常见问题

#### ❓ 端口被占用怎么办？
```bash
# 清理3001端口（后端）
lsof -ti:3001 | xargs kill -9

# 清理3000端口（前端）
lsof -ti:3000 | xargs kill -9
```

#### ❓ 启动时报错 "Cannot find module"？
```bash
# 清理并重新安装依赖
rm -rf node_modules client/node_modules
rm package-lock.json client/package-lock.json
./setup.sh
```

#### ❓ 需要配置数据库吗？
不需要！系统默认使用模拟数据，无需配置数据库即可运行。如果您想连接真实数据库，请编辑 `.env` 文件中的数据库配置。

#### ❓ 更多问题？
查看详细的故障排除指南: [TROUBLESHOOTING.md](./TROUBLESHOOTING.md)

### 快速测试

安装并启动后，运行快速测试脚本验证所有API端点：

```bash
./quick-test.sh
```

## 📋 优先级划分

| 优先级 | 天数 | 说明 | 颜色 |
|--------|------|------|------|
| P1 | < 7天 | 立即扩容 | 🔴 红色 |
| P2 | 7-13天 | 近期扩容 | 🟡 橙色 |
| P3 | 14-29天 | 计划扩容 | 🔵 蓝色 |
| P4 | ≥30天 | 暂无压力 | 🟢 绿色 |

## 🎨 界面特色

- **响应式设计**: 支持桌面和移动设备
- **实时数据**: 动态更新容量指标
- **交互式卡片**: 可展开查看详细信息
- **视觉化指示**: 颜色编码的优先级和风险等级
- **统计汇总**: 各角色视角的关键指标统计

## 🔧 扩容方案示例

| 组件类型 | 扩容方案 | 预期效果 | 工作量 |
|----------|----------|----------|--------|
| API Gateway | 添加网关节点，实现水平扩展 | 50%容量提升 | 16小时 |
| DB Connection Pool | 增加连接池大小，优化查询 | 30%容量提升 | 12小时 |
| Redis Cache | 扩展缓存集群，优化淘汰策略 | 40%容量提升 | 8小时 |
| Load Balancer | 升级配置，添加健康检查 | 25%容量提升 | 6小时 |
| Message Queue | 扩展工作进程，优化处理逻辑 | 60%容量提升 | 10小时 |

## 📈 业务价值

1. **快速决策**: 各角色都能快速定位需要扩容的资源
2. **风险控制**: 提前识别容量风险，避免业务中断
3. **资源优化**: 合理安排扩容时间和资源
4. **效率提升**: 标准化的扩容流程和执行清单
5. **成本控制**: 基于数据驱动的扩容决策

## 🔄 数据流程

1. **数据采集**: 从 `metrics_daily` 表提取容量指标
2. **优先级计算**: 基于 `days_to_saturation` 计算优先级
3. **扩容建议**: 根据组件类型生成扩容方案
4. **效果预测**: 评估扩容后的容量改善
5. **多视角展示**: 为不同角色提供定制化报表

## 🛠️ 开发说明

### 项目结构
```
├── server/                 # 后端代码
│   ├── controllers/        # 控制器
│   ├── routes/            # 路由
│   └── index.js           # 服务器入口
├── client/                # 前端代码
│   ├── src/
│   │   ├── components/    # React组件
│   │   ├── services/      # API服务
│   │   └── App.tsx        # 应用入口
│   └── package.json
└── package.json           # 根依赖
```

### 生产部署
```bash
# 构建前端
npm run build

# 启动生产服务器
npm start
```

## 📞 支持与反馈

如有问题或建议，请提交Issue或联系开发团队。

---

**注意**: 当前版本使用模拟数据进行演示，生产环境需要连接真实的 `metrics_daily` 数据表。